export interface ItemFilter {
  tipo: string;
  nome: string;
  quantidade: number;
  bonus: number;
  created: Date;
}

export interface ItemElement {
  tipo: string;
  nome: string;
  quantidade: number;
  bonus: number;
  created: Date;
}
